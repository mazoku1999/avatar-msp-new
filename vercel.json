{
    "version": 2,
    "installCommand": "pnpm install",
    "builds": [
        {
            "src": "apps/demo/next.config.js",
            "use": "@vercel/next"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "apps/demo/$1"
        }
    ]
}